class Engine {
    constructor(world, redBugs, blackBugs, cycles) {
      this.world = world;
      this.redBugs = redBugs;
      this.blackBugs = blackBugs;
      this.cycles = cycles;
    }
  }

export { Engine };
